apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    project: roboshop
spec:
  replicas: 1
  selector:
    matchLabels:
      tier: web
      component: catalogue
      environment: dev
  template:
    metadata:
      labels:
        tier: web
        component: catalogue
        environment: dev
    spec:
      containers:
      - name: catalogue
        image: venkat1095/catalogue:1.0.0
        envFrom:
          configMapRef:
            name: catalogue
---
apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
spec:
  selector:
    tier: web
    component: catalogue
    environment: dev
  ports:
  - name: catalogue-port
    protocol: TCP
    port: 8080
    targetPort: 8080


---
apiVersion: v1
kind: ConfigMap
metadata:
  name: catalogue
  namespace: roboshop
data:
  MONGO: "true"
  MONGO_URL: "monogodb://monogodb:27017/catalogue"